import 'package:flutter/material.dart';

class AuctionManagementView extends StatelessWidget {
  const AuctionManagementView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Auction Management Screen'));
  }
}
