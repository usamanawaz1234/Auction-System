import 'package:flutter/material.dart';

class BidMonitoringView extends StatelessWidget {
  const BidMonitoringView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Bid Monitoring Screen'));
  }
}
